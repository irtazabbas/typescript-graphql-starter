{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "DB_USER",
  "password": "DB_PASSWORD",
  "database": "DB_NAME",
  "synchronize": true,
  "logging": false,
  "entities": [
     "src/model/*/*.entity.ts"
  ],
  "migrations": [
     "src/migration/**/*.ts"
  ],
  "subscribers": [
     "src/subscriber/**/*.ts"
  ],
  "cli": {
     "migrationsDir": "src/migration",
     "subscribersDir": "src/subscriber"
  }
}